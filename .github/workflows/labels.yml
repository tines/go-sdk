name: Labels
on:
    pull_request:
        types:
            - opened
            - labeled
            - unlabeled
jobs:
    check:
        runs-on: ubuntu-latest
        name: Require Label
        steps:
            - uses: actions/github-script@v7
              with:
                  script: |
                    const labels = github.event.pull_request.title
                    console.log(labels)